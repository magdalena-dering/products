import firebase from "firebase";
const config = {
  apiKey: "X",
  authDomain: "X",
  databaseURL: "X",
  projectId: "X",
  storageBucket: "",
  messagingSenderId: "X",
  appId: "X"
};
const fire = firebase.initializeApp(config);
export default fire;
